# The offline frontend demo requires no environment variables.
# Enable the optional external Mapbox mode only with a browser-safe public token.
NEXT_PUBLIC_MAPBOX_ENABLED=false
NEXT_PUBLIC_MAPBOX_ACCESS_TOKEN=
